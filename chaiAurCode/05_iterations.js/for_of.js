//// # for of loop.

const arr = [1, 2, 3, 4, 5]

// for (const element of arr) {
//      console.log(element);
// }

const greet = "Hello_JavaScript"
// for (const gr of greet) {
//      console.log(gr);
// }

const map = new Map();
map.set('js', 'javascript')
map.set('cpp', 'c++')
map.set('cy', 'Python')

// console.log(map);

// for (const [key, val] of map) {
//      console.log(`${key} :::: ${val}`);
// }

const obj = {
     g1: "NFS",
     g2: "GTA"
}
// for (const element of obj) { //obj is not iterable
//      console.log(element);
// }

